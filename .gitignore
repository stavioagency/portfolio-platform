# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/

# Environment variables (never commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# Supabase CLI local state, written by `supabase link`. Machine-specific, and
# `pooler-url` carries a database connection string — this is the .env of the
# CLI and belongs in the repo no more than one does.
supabase/.temp/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
